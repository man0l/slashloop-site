# slashloop-site

Marketing landing for [slashloop](https://github.com/man0l/slashloop). React + Vite + Tailwind.

```bash
npm install
npm run dev      # local dev
npm run build    # production build -> dist/
```

Deployed on Vercel.
